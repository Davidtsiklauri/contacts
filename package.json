{
  "scripts": {
    "install-dependencies": "npm i --prefix ./back-end && npm i --prefix ./front-end",
    "start-backend": "npm run start --prefix ./back-end",
    "start-front-end": "npm run start --prefix ./front-end"
  }
}
